node_modules/
*.tgz
.DS_Store
coverage/
*.log

# Local agent state index (per-machine working notes, not part of the repo).
CODEX_STATE.md
references/

# Session-local agent state (todo tool task lists etc.).
.pi/

# npm lockfile is machine-local (pi installs the git package with npm install).
package-lock.json

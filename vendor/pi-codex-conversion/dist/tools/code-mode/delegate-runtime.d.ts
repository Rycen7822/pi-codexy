import { type DelegateRequestMessage } from "./host-protocol.js";
import { CodeModeNestedRenderStore } from "./trace-render-state.js";
import type { CodeModeToolDefinition, RuntimeResponse, ToolExecutionContext } from "./types.js";
type SendMessage = (message: unknown) => void;
export declare class CodeModeDelegateRuntime {
    private readonly traceRuntimeGeneration;
    private readonly cellContexts;
    private readonly cellTools;
    private readonly controllers;
    private readonly notifications;
    private readonly execSessions;
    private readonly blockers;
    private readonly blockerChanges;
    private readonly sequentialTails;
    private readonly traces;
    private readonly cleanupTimers;
    private readonly send;
    private readonly renderStore;
    constructor(send: SendMessage, renderStore?: CodeModeNestedRenderStore);
    bindCell(cellId: string, context: ToolExecutionContext, tools?: Map<string, CodeModeToolDefinition>): void;
    updateCellContext(cellId: string, context: ToolExecutionContext): void;
    closeCell(cellId: string): void;
    clear(): void;
    isBlocked(cellId: string): boolean;
    waitUntilUnblocked(cellId: string, signal?: AbortSignal): Promise<void>;
    cancel(id: number): void;
    cancelCell(cellId: string): void;
    handleRequest(message: DelegateRequestMessage): void;
    invokeDirect(cellId: string, requestId: number, toolName: string, input: unknown): Promise<unknown>;
    notifyDirect(cellId: string, value: string): void;
    attach(response: RuntimeResponse): RuntimeResponse;
    private invoke;
    private invokeTool;
    private recordExecSession;
    private invokeSequential;
    private setBlocked;
    private handleNotification;
    private respond;
}
export {};
